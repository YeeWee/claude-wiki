---
title: "第二十四章：API 客户端服务"
type: source
created: 2026-04-15
updated: 2026-04-15
sources: [chapter-24-api客户端服务.md]
related:
  - ../concepts/api-client.md
  - ../concepts/stream-processing.md
  - ../concepts/token-tracking.md
---

# 第二十四章：API 客户端服务

## 概要

API 客户端服务是 Claude Code 与 Claude API 通信的核心层，负责请求构建、身份认证、流式响应处理、错误处理与重试、Token 追踪等功能。

## 核心要点

### 目录结构

```
src/services/api/
├── claude.ts          # 核心 API 请求逻辑 (126KB)
├── client.ts          # Anthropic SDK 客户端创建
├── errors.ts          # 错误类型定义与处理
├── withRetry.ts       # 重试策略实现
├── logging.ts         # API 请求日志记录
├── usage.ts           # 配额使用追踪
├── filesApi.ts        # Files API 操作
├── bootstrap.ts       # 启动时 API 验证
└── promptCacheBreakDetection.ts  # 缓存失效检测
```

### Provider 支持

| Provider | 环境变量 | SDK | 认证方式 |
|----------|----------|-----|----------|
| First Party | 默认 | `Anthropic` | API Key / OAuth |
| Bedrock | `CLAUDE_CODE_USE_BEDROCK` | `AnthropicBedrock` | AWS Credentials |
| Vertex | `CLAUDE_CODE_USE_VERTEX` | `AnthropicVertex` | GCP Credentials |
| Foundry | `CLAUDE_CODE_USE_FOUNDRY` | `AnthropicFoundry` | Azure AD |

### 流式响应处理

流事件处理流程：
- `message_start`：记录 TTFB，初始化 usage
- `content_block_start`：初始化 contentBlock
- `content_block_delta`：累积文本/工具输入
- `content_block_stop`：创建 AssistantMessage 并 yield
- `message_delta`：更新 usage, stop_reason，计算成本

### 错误处理与重试

| 错误类型 | 错误消息 | 处理策略 |
|----------|----------|----------|
| `authentication_error` | `invalid x-api-key` | 重新认证 |
| `permission_denied` | `OAuth token revoked` | 返回登录提示 |
| `rate_limit_error` | 429 状态码 | 等待 retry-after |
| `overloaded_error` | 529 状态码 | 指数退避重试 |
| `prompt_too_long` | 上下文超限 | 触发 compact |
| `timeout_error` | 请求超时 | 非流式降级 |

### 流式处理优化

- **Stream Watchdog**：超过 90 秒无数据则中断流，触发非流式降级
- **Stall 检测**：30 秒阈值检测流中断
- **非流式降级**：流失败时自动降级到非流式请求

### Token 追踪

`NonNullableUsage` 类型包含：
- `input_tokens`、`output_tokens`
- `cache_creation_input_tokens`、`cache_read_input_tokens`
- `ephemeral_1h_input_tokens`、`ephemeral_5m_input_tokens`
- `server_tool_use`（web_search_requests、web_fetch_requests）

### Fast Mode 降级

遇到 429/529 时：
- 短延迟（< SHORT_RETRY_THRESHOLD_MS）：保持 fast mode 重试
- 长延迟：进入 cooldown，切换标准速度

## 源文件位置

- `src/services/api/claude.ts`：核心 API 请求逻辑
- `src/services/api/client.ts`：Anthropic SDK 客户端创建
- `src/services/api/withRetry.ts`：重试策略实现
- `src/services/api/errors.ts`：错误类型定义
- `src/services/api/logging.ts`：API 日志记录
- `src/utils/modelCost.ts`：成本计算函数

## Connections

- [API Client](../concepts/api-client.md) - API 通信层概念
- [流式处理](../concepts/stream-processing.md) - SSE 流处理机制
- [Token 追踪](../concepts/token-tracking.md) - 使用量追踪

## Open Questions

- FOREGROUND_529_RETRY_SOURCES 的完整列表？
- SHORT_RETRY_THRESHOLD_MS 的具体值？
- 多 Provider 场景下的切换策略？

## Sources

- `chapters/chapter-24-api客户端服务.md`